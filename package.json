{
  "name": "mono",
  "packageManager": "yarn@3.5.0",
  "private": true,
  "scripts": {
    "build": "yarn workspaces foreach run build",
    "dev": "yarn workspace site dev",
    "preview": "yarn workspace site preview"
  },
  "workspaces": [
    "packages/*"
  ]
}
